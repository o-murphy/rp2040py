"""`--board-spec` targets for CircuitPython boards - see `boards/__init__.py`.

Directory names here are CircuitPython's own board ids (lowercase, as in
`ports/raspberrypi/boards/<board>/` and in the `downloads.circuitpython.org` URL path), the same
"name it after the firmware's own id" rule `boards/micropython/` follows with MicroPython's
(uppercase) ones - so a board that exists for both firmwares appears twice, once per directory,
under each firmware's own spelling.
"""
